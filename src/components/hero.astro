---
import loadConfig from '../configLoader';

const config = await loadConfig();

const { ticketUrl } = config;
---

<section
  class="relative bg-cover bg-center"
  style={`background-image: url('${import.meta.env.BASE_URL}/images/banner.jpg');`}
>
  <div class="container max-w-4xl mx-auto px-8 py-48 flex flex-col h-full">
    <p class="text-brand-extra font-bold mb-4 text-xl font-header uppercase">
      New event coming up in October!
    </p>
    <h1
      class="text-white text-4xl md:text-5xl uppercase font-header font-bold mb-4"
    >
      The Conference with a Focus on .NET MAUI 🏝️
    </h1>
    <p class="text-white text-lg mt-6 mb-12">
      .NET MAUI day is a one-day, free, event filled with sessions that have to
      do with cross-platform development based on .NET MAUI (Multi-platform App
      UI). Speakers from all around the world will be there to present about the
      latest and greatest in the .NET MAUI ecosystem, as well as being in the
      “hallway track” so you can talk to them, discuss and ask questions.
    </p>
    <div class="flex space-x-4">
      <a
        href={`${ticketUrl}`}
        class="bg-brand-extra font-bold text-white px-6 py-3 rounded-full text-md hover:bg-brand-extraDark"
        >Get a Ticket</a
      >
      <a
        href="#speakers"
        class="border font-bold border-white text-white px-6 py-3 rounded-full text-md hover:bg-white hover:text-brand-dark"
        >View Speakers</a
      >
    </div>
  </div>
</section>
