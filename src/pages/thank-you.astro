---
import Layout from "../layouts/Layout.astro";
---

<Layout
  pageTitle="Thank you | MAUI Day"
  pageDescription="Thanks for subscribing to the MAUI Day newsletter."
>
  <section class="relative bg-brand-dark text-white min-h-screen flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 bg-black/30"></div>
    <div
      class="absolute inset-0 z-0"
      style={{
        background: "radial-gradient(125% 125% at 50% 10%, transparent 40%, #C93CAD77 100%)",
      }}
    />
    <div
      class="absolute inset-0 z-0"
      style={{
        backgroundImage: `
          linear-gradient(to right, #e7e5e411 1px, transparent 1px),
          linear-gradient(to bottom, #e7e5e411 1px, transparent 1px)
        `,
        backgroundSize: "20px 20px",
        backgroundPosition: "0 0, 0 0",
        maskImage: `
          repeating-linear-gradient(
            to right,
            black 0px,
            black 3px,
            transparent 3px,
            transparent 8px
          ),
          repeating-linear-gradient(
            to bottom,
            black 0px,
            black 3px,
            transparent 3px,
            transparent 8px
          )
        `,
        WebkitMaskImage: `
          repeating-linear-gradient(
            to right,
            black 0px,
            black 3px,
            transparent 3px,
            transparent 8px
          ),
          repeating-linear-gradient(
            to bottom,
            black 0px,
            black 3px,
            transparent 3px,
            transparent 8px
          )
        `,
        maskComposite: "intersect",
        WebkitMaskComposite: "source-in",
      }}
    />

    <div class="container relative z-10 max-w-2xl mx-auto px-4 text-center py-20">
      <div class="mb-8 flex justify-center">
        <div class="relative">
          <div class="w-24 h-24 bg-gradient-to-br from-pink-500 to-purple-600 rounded-full flex items-center justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-12 h-12 text-white" viewBox="0 0 24 24"><path fill="currentColor" d="M9 16.17L4.83 12l-1.42 1.41L9 19L21 7l-1.41-1.41z"/></svg>
          </div>
        </div>
      </div>
      
      <h1 class="text-5xl md:text-6xl mb-6 font-header font-bold uppercase bg-gradient-to-r from-pink-400 to-purple-400 bg-clip-text text-transparent">
        You're All Set!
      </h1>
      
      <p class="text-xl text-white mb-4">
        Thanks for subscribing to the MAUI Day newsletter.
      </p>
      
      <p class="text-lg text-white max-w-xl mx-auto mb-12">
        We'll keep you in the loop with all the latest updates about speakers, schedule, and everything else MAUI Day. Get ready for an amazing event! 🎉
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href="/"
          class="btn-gradient px-8 py-4 rounded-full font-bold text-white shadow-lg shadow-pink-500/30 hover:shadow-pink-500/50 transform hover:-translate-y-0.5 transition-all duration-300 inline-flex items-center justify-center gap-2 group"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="size-5 group-hover:-translate-x-1 transition-transform" viewBox="0 0 24 24"><path fill="currentColor" d="M20 11H7.83l5.59-5.59L12 4l-8 8l8 8l1.41-1.41L7.83 13H20z"/></svg>
          <span>Back to Home</span>
        </a>
        
        <a
          href="/#sponsors"
          class="px-8 py-4 rounded-full font-bold text-white border-2 border-white/20 hover:border-white hover:bg-white/5 transition-all duration-300 inline-flex items-center justify-center gap-2"
        >
          <span>Check Out Our Sponsors</span>
        </a>
      </div>
    </div>
  </section>
</Layout>
